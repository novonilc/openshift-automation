---
#### Create Change Requesting Using Service Now API
- name: Generated by
  shell: getent passwd "{{ lookup('env','USER') }}" | cut -f 5 -d ":"
  register: gen_user
  run_once: true
  tags:
    - changecr
    - querycr
    - report_user

- include_vars: "{{operation}}.yml"
  tags:
    - patch_variables
    - decom_variables
    - general_variables 

- include: change_request.yml
  tags:
    - patch
    - decom
    - general 
